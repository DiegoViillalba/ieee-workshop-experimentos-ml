# requirements.txt
# Generado con: uv pip freeze > requirements.txt
# Para reproducir exactamente: uv pip sync requirements.txt
#
# RECOMENDADO: usar pyproject.toml con uv (ver README).
# Este archivo existe como fallback para Colab y entornos sin uv:
#   !pip install -r requirements.txt

# ── Deep Learning ────────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Datos y métricas ─────────────────────────────────────────────────────────
numpy>=1.24.0
scikit-learn>=1.3.0
Pillow>=9.0.0

# ── Configuración ────────────────────────────────────────────────────────────
PyYAML>=6.0

# ── Visualización ────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Logging de experimentos ──────────────────────────────────────────────────
wandb>=0.16.0

# ── Pipeline reproducible ─────────────────────────────────────────────────────
dvc>=3.0.0

# ── Testing y calidad de código ───────────────────────────────────────────────
pytest>=7.4.0
ruff>=0.4.0
